BOT_TOKEN=
# 🇺🇦 Основний токен Telegram‑бота, який видає BotFather. Без нього бот не запуститься.
# 🇬🇧 Main Telegram bot token issued by BotFather. Required for the bot to start.

# --- База даних / Database ---
DATABASE_URL=postgresql+asyncpg://app_user:change_me@db:5432/booking_app
# 🇺🇦 Адреса підключення до бази даних (PostgreSQL + asyncpg)
# 🇬🇧 Connection string for the database (PostgreSQL + asyncpg)

# --- Технічні параметри / Technical ---
TELEGRAM_PAYMENT_PROVIDER_TOKEN=
# 🇺🇦 Токен платіжного провайдера Telegram (отримується у BotFather)
# 🇬🇧 Telegram payment provider token (issued via BotFather)

# --- Адміністратори / Admins ---
ADMIN_IDS=
# 🇺🇦 Список Telegram ID адміністраторів для отримання повідомлень
# 🇬🇧 List of Telegram IDs of admins who receive notifications

# --- Локалізація / Localization ---
DEFAULT_LANGUAGE=en
# 🇺🇦 Мова інтерфейсу за замовчуванням (наприклад: uk, en)
# 🇬🇧 Default interface language (e.g., uk, en)

DEFAULT_CURRENCY=USD
# 🇺🇦 Валюта за замовчуванням для цін і платежів
# 🇬🇧 Default currency for prices and payments

LOCAL_TIMEZONE=Europe/Kyiv
# 🇺🇦 Часовий пояс сервера
# 🇬🇧 Server timezone

# --- Telegram Mini App / WebApp ---
TWA_WEBAPP_URL=
# 🇺🇦 Публічний HTTPS URL фронтенду Mini App (використовуйте ngrok для розробки, наприклад, https://<id>.ngrok-free.app)

TWA_ALLOW_ALL_ORIGINS=false
# 🇺🇦 Встановіть true лише для локальної розробки, якщо вам потрібен wildcard CORS; в іншому випадку залиште false

BUSINESS_TIMEZONE=Europe/Kyiv
# 🇺🇦 Часовий пояс бізнесу (салону), використовується для клієнтів
# 🇬🇧 Business timezone (salon), used for client-facing times

# --- Кеш / пагінація / Cache & Pagination ---
SETTINGS_CACHE_TTL_SECONDS=60
# 🇺🇦 Час життя кешу налаштувань у секундах
# 🇬🇧 Settings cache time-to-live in seconds

PAGINATION_PAGE_SIZE=5
# 🇺🇦 Кількість елементів на сторінку при пагінації
# 🇬🇧 Number of items per page for pagination

TWA_WEBAPP_URL=https://your-domain.com
# 🇺🇦 Адреса вашого Telegram WebApp (Mini App), який відкривається у клієнта.
# 🇬🇧 URL of your Telegram WebApp (Mini App) that opens for the client.

TWA_ALLOW_ALL_ORIGINS=true
# 🇺🇦 Дозволити доступ до WebApp з усіх джерел (CORS). Використовується для спрощення інтеграції.
# 🇬🇧 Allow WebApp access from all origins (CORS). Useful for easier integration.